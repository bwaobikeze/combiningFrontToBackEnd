<!DOCTYPE html>
<html lang="en">
<meta charset="UTF-8">
  <style>
  h1 {
  padding:40px;
    text-align: center;
  }
  #userinfo{
	width: 300px;
	height: 30px;
  }
  .login{
	position: absolute;
	top: 35%;
	left: 50%;
	transform: translate(-50%, -50%);
  }
  </style>
  <h1>Fuel Quote History</h1>
  <form action="/quotehist" method="POST">
  <body style="background-color:beige;">
  <div class="login">
  <style type="text/css">
.tg  {border-collapse:collapse;border-spacing:0;}
.tg td{border-color:black;border-style:solid;border-width:1px;font-family:Arial, sans-serif;font-size:14px;
  overflow:hidden;padding:10px 5px;word-break:normal;}
.tg th{border-color:black;border-style:solid;border-width:1px;font-family:Arial, sans-serif;font-size:14px;
  font-weight:normal;overflow:hidden;padding:10px 5px;word-break:normal;}
.tg .tg-0pky{border-color:inherit;text-align:left;vertical-align:top}
</style>
<table class="tg">
<thead>
  <tr>
    <th class="tg-0pky">Gallons Requested</th>
    <th class="tg-0pky">Delivery Address</th>
    <th class="tg-0pky">Delivery Date</th>
    <th class="tg-0pky">Suggested Price</th>
    <th class="tg-0pky">Total Amount Due</th>
  </tr>
</thead>
<tbody>
  <% for(let i=0; i<QuoteHist.length;i++) {%>
  <tr>
    <td class="tg-0pky"><%= QuoteHist[i].gallonreq %></td>
    <td class="tg-0pky"><%= QuoteHist[i].deliveryaddress%></td>
    <td class="tg-0pky"><%= QuoteHist[i].deliverydate%></td>
    <td class="tg-0pky"><%= QuoteHist[i].suggestedpricepergallon %></td>
    <td class="tg-0pky"><%= QuoteHist[i].totalamountdue%></td>
  </tr>
  <% } %>
</tbody>
</table>
  </div>
</form>
  </body>
  <div>
  <button onclick="location.href='/quoteform'"+>Check User Quote Form</button>
  <form action="/logout" method="POST">
    <button type="submit">Log Out</button>
  </form>
</div>
<form action="/index" method="POST">
	<button type="submit">Main Page</button>
  </form>
</html>